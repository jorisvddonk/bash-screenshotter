# Bash.org quote screenshotter

Makes screenshots of the top quotes on bash.org, via [Puppeteer](https://github.com/GoogleChrome/puppeteer/)

## Usage

1. `npm install`
2. `node fetch_screenshots.js`
